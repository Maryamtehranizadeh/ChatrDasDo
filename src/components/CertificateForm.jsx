import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { getCertificates } from "../utils/getAll";

function CertificateForm() {
  return <div>CertificateForm</div>;
}

export default CertificateForm;
